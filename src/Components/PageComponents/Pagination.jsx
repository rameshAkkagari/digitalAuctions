// import React from 'react'

function Pagination() {
    // const pagination = []
    
    // for (let i = 1; i < Math.ceil(publickAuctions.length/10)+1; i++){
    //     pagination.push(i)
    // }
  return (
    <div>
      
    </div>
    // <div>
    //     {pagination.map((page,i)=><button key={i} onClick={()=>PageHandlerData(page)}>{page}</button>)}
    // </div>
  )
}

export default Pagination
// {publickAuctions,PageHandlerData}
// import React from 'react'

// function Pagination({ publickAuctions, PageHandlerData }) {

//   const pagination = [];
//   const totalPages = Math.ceil(publickAuctions.pagination.total_records / 10);

//   for (let i = 1; i <= totalPages; i++) {
//     pagination.push(i);
//   }
//   return (
//     <div>
//        {/* {pagination.map((page, i) => (
//         <button key={i} onClick={() => PageHandlerData(page)}>
//           {page}
//         </button>
//       )} */}
//       {pagination.map((page,i)=><button key={i} onClick={()=>PageHandlerData()}>{page}</button>)}
//     </div>
//   )
// }

// export default Pagination